import { h } from 'preact'
import { Fragment, FunctionComponent } from 'preact'

export const HostPage: FunctionComponent<{ matches: { slug: string } }> = ({ matches: { slug } }) => {
  return <Fragment></Fragment>
}
